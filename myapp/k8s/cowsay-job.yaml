apiVersion: batch/v1
kind: Job
metadata:
  name: cowsay-job
spec:
  template:
    spec:
      containers:
      - name: cowsay
        image: pubg55kai/my-cowsay:latest
        imagePullPolicy: IfNotPresent
      restartPolicy: Never
  backoffLimit: 3
